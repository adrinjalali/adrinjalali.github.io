# adrin.info

Personal blog built with [Pelican](https://getpelican.com/).

## Development

```bash
uv sync        # Install dependencies
make serve     # Local dev server at http://localhost:8000
make build     # Production build
```

## Deployment

Push to `source` branch triggers automatic deployment via GitHub Actions.
